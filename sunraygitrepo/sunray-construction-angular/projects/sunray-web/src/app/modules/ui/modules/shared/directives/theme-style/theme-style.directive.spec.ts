import { ThemeStyleDirective } from './theme-style.directive';

describe('ThemeStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeStyleDirective();
    expect(directive).toBeTruthy();
  });
});
