/*
import { componentsMap } from './system-components'

export class SystemComponentsService {
  constructor() {
    this.subscribeToSystemComponents()
  }

  private subscribeToSystemComponents() {
    this.store.select(systemComponents)
      .subscribe(components => {
        this.handleSystemComponents(components)
      })
  }

  private handleSystemComponents(components) {

  }
}

*/
