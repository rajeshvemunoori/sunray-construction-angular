import { selector } from './for-taxonomy/selector';
import { isValid } from './for-taxonomy/is-valid';
export declare const forTaxonomy: {
    selector: typeof selector;
    isValid: typeof isValid;
};
