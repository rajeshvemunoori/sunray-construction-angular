import { FormControl as NgFormControl } from '@angular/forms';
import { iFormControl } from '../../interfaces/index';
export declare class FormControlFactory {
    build(formControl: iFormControl): NgFormControl;
}
